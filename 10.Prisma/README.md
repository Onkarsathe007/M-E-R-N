#Prima: 

### Install a Prisma:

```
npx install prisma --datasource-provider mysql 
```
Once we run this command then we will get the 

```
Prisma/ 
    - schema.prisma
.env
.gitignore
prisma.config.ts
```
